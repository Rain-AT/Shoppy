// import { fromJS } from 'immutable';
// import { makeSelectItemListDomain } from '../selectors';

// const selector = makeSelectItemListDomain();

describe('makeSelectItemListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

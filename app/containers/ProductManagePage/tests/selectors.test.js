// import { fromJS } from 'immutable';
// import { makeSelectProductManagePageDomain } from '../selectors';

// const selector = makeSelectProductManagePageDomain();

describe('makeSelectProductManagePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
